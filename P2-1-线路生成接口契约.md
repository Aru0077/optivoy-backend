# P2-1 线路生成接口契约（冻结草案）

版本：`v1.2`  
日期：`2026-04-01`  
状态：`Backend Phase 5 已同步`

## 1. 范围

本文件冻结以下契约：

- backend `POST /trip-planner/generate` 请求/响应结构
- backend -> optimizer `/solve` 请求结构
- `transit_cache` 表结构（有向边模型）
- 到达机场缓冲时间规则

不包含：

- 高德调用调度细节（重试/限流策略在后续阶段）

## 2. Backend 生成接口契约

接口：`POST /trip-planner/generate`

请求体（冻结）：

```json
{
  "city": "Beijing",
  "province": "Beijing",
  "arrivalDateTime": "2026-07-15T14:00:00+08:00",
  "arrivalBufferMinutes": 90,
  "selectedPointIds": ["uuid-1", "uuid-2"],
  "paceMode": "standard",
  "hotelMode": "multi",
  "mealPolicy": "auto",
  "arrivalAirportCode": "PEK",
  "departureAirportCode": "PEK"
}
```

字段说明：

- `selectedPointIds`：仅允许 `spot/shopping/restaurant`
- `paceMode`：`light|standard|compact`
- `hotelMode`：`single|multi`
- `mealPolicy`：`auto|off`
- `arrivalBufferMinutes`：`60-120`，默认 `90`

响应体（冻结骨架）：

```json
{
  "city": "Beijing",
  "province": "Beijing",
  "arrivalDateTime": "2026-07-15T14:00:00+08:00",
  "arrivalBufferMinutes": 90,
  "tripDays": 4,
  "solverStatus": "FEASIBLE",
  "days": [
    {
      "dayNumber": 1,
      "date": "2026-07-15",
      "hotel": {
        "id": "hotel-uuid",
        "name": "Hotel Name",
        "bookingUrl": "https://..."
      },
      "legs": [
        {
          "fromPointId": "uuid-a",
          "toPointId": "uuid-b",
          "transportMode": "transit",
          "transitMinutes": 35,
          "drivingMinutes": 22,
          "walkingMeters": 1300,
          "distanceKm": 9.7,
          "transitSummary": "地铁4号线 -> 地铁1号线"
        }
      ],
      "points": [
        {
          "id": "uuid-a",
          "pointType": "spot",
          "name": "...",
          "suggestedDurationMinutes": 120,
          "guideI18n": {
            "zh-CN": "..."
          }
        }
      ]
    }
  ],
  "links": {
    "outboundFlight": "https://...",
    "returnFlight": "https://..."
  }
}
```

## 3. 到达缓冲时间规则

- 首日可用开始时间：`firstDayUsableStart = max(arrivalDateTime + arrivalBufferMinutes, paceStartTime)`
- 若 `firstDayUsableStart >= paceEndTime`：首日不排游玩点，仅安排入城/入住
- 缓冲时间不替代 `mealPolicy`，两者独立生效

## 4. Optimizer 接口契约

接口：`POST /solve`

请求体（冻结）：

```json
{
  "city": "Beijing",
  "province": "Beijing",
  "arrivalAirportCode": "PEK",
  "departureAirportCode": "PEK",
  "switchPenaltyMinutes": 20,
  "newHotelPenaltyMinutes": 90,
  "arrivalDateTime": "2026-07-15T14:00:00+08:00",
  "airportBufferMinutes": 90,
  "arrivalAirportBufferMinutes": 40,
  "departureAirportBufferMinutes": 120,
  "paceMode": "standard",
  "hotelMode": "multi",
  "mealPolicy": "auto",
  "points": [
    {
      "id": "uuid-a",
      "pointType": "spot",
      "suggestedDurationMinutes": 120,
      "latitude": 39.9,
      "longitude": 116.4,
      "arrivalAnchor": {
        "latitude": 39.9001,
        "longitude": 116.4001
      },
      "departureAnchor": {
        "latitude": 39.901,
        "longitude": 116.401
      },
      "openingHoursJson": [
        {
          "weekday": "mon",
          "start": "09:00",
          "end": "17:30"
        }
      ],
      "specialClosureDates": ["2026-10-01"],
      "lastEntryTime": "16:30",
      "reservationRequired": true,
      "reservationCutoffMinutes": 120,
      "reservationTimeSlotsJson": ["09:00", "13:00"],
      "queueProfileJson": {
        "weekdayMinutes": 20,
        "weekendMinutes": 45,
        "holidayMinutes": 70
      },
      "bestVisitWindowsJson": [
        {
          "start": "18:00",
          "end": "20:00",
          "tag": "sunset"
        }
      ]
    }
  ],
  "hotels": [
    {
      "id": "hotel-uuid",
      "latitude": 39.9,
      "longitude": 116.4,
      "arrivalAnchor": {
        "latitude": 39.9002,
        "longitude": 116.4002
      },
      "departureAnchor": {
        "latitude": 39.9003,
        "longitude": 116.4003
      },
      "foreignerFriendly": true,
      "checkInTime": "15:00",
      "checkOutTime": "12:00",
      "bookingStatus": "available",
      "bookableDatesJson": ["2026-07-15", "2026-07-16"],
      "pricePerNightMinCny": 480,
      "pricePerNightMaxCny": 780
    }
  ],
  "distanceMatrix": {
    "rows": [
      {
        "fromPointId": "uuid-a",
        "toPointId": "hotel-uuid",
        "transitMinutes": 35,
        "drivingMinutes": 22,
        "walkingMeters": 1300,
        "walkingMinutes": 16,
        "distanceKm": 9.7,
        "transitSummary": "地铁4号线 -> 地铁1号线"
      }
    ]
  },
  "objective": "min_days_then_transit",
  "maxDays": 14,
  "timeLimitSeconds": 2.5,
  "maxIterations": 4,
  "badDayTransitMinutesThreshold": 180,
  "badDayPenaltyMinutes": 120
}
```

说明：

- `distanceMatrix` 来自 backend 当前读取到的 `transit_cache` ready 边，并额外补充了 `walkingMinutes`
- `points/hotels` 已开始向 optimizer 透传结构化营业时间、预约、排队、锚点与酒店可订信息
- `arrivalAirportBufferMinutes/departureAirportBufferMinutes` 来自机场基础数据，用于后续 optimizer 建模
- 当矩阵缺边时，optimizer 会做坐标兜底估算
- `transitMinutes` 现在优先写入真实公交时间；当公交接口失败时，使用独立的公交兜底估算，并通过 `transitProviderStatus` 标记，而不再混用驾车/步行最小值

## 5. transit_cache 表结构（冻结）

当前阶段仍采用**有向边**模型：`fromPointId -> toPointId`。

- 对同一城市，A->B 与 B->A 分开存储
- 唯一键：`(city, fromPointId, toPointId)`

SQL DDL（冻结）：

```sql
CREATE TABLE IF NOT EXISTS transit_cache (
  id uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  city varchar(120) NOT NULL,
  province varchar(120),
  "fromPointId" uuid NOT NULL,
  "fromPointType" varchar(16) NOT NULL,
  "toPointId" uuid NOT NULL,
  "toPointType" varchar(16) NOT NULL,
  "transitMinutes" int NOT NULL,
  "drivingMinutes" int NOT NULL,
  "walkingMeters" int NOT NULL,
  "walkingMinutes" int NOT NULL,
  "transitSummary" text,
  "transitSummaryI18n" jsonb,
  "transitProviderStatus" varchar(16),
  "distanceKm" double precision NOT NULL,
  provider varchar(24) NOT NULL DEFAULT 'amap',
  status varchar(16) NOT NULL DEFAULT 'ready',
  "expiresAt" timestamp,
  "createdAt" timestamp NOT NULL DEFAULT now(),
  "updatedAt" timestamp NOT NULL DEFAULT now(),
  CONSTRAINT chk_transit_cache_from_to_not_equal CHECK ("fromPointId" <> "toPointId"),
  CONSTRAINT chk_transit_cache_from_point_type CHECK ("fromPointType" IN ('spot','shopping','restaurant','hotel','airport')),
  CONSTRAINT chk_transit_cache_to_point_type CHECK ("toPointType" IN ('spot','shopping','restaurant','hotel','airport')),
  CONSTRAINT chk_transit_cache_status CHECK (status IN ('ready','stale','failed'))
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_transit_cache_city_from_to
ON transit_cache (city, "fromPointId", "toPointId");
CREATE INDEX IF NOT EXISTS idx_transit_cache_city ON transit_cache (city);
CREATE INDEX IF NOT EXISTS idx_transit_cache_from ON transit_cache ("fromPointId");
CREATE INDEX IF NOT EXISTS idx_transit_cache_to ON transit_cache ("toPointId");
CREATE INDEX IF NOT EXISTS idx_transit_cache_updated_at ON transit_cache ("updatedAt");
```

## 6. JSON Schema（P4 完成）

- `src/modules/trip-planner/contracts/optimizer-solve-request.schema.json`
- `src/modules/trip-planner/contracts/optimizer-solve-response.schema.json`

## 7. 实现映射

- TypeORM 实体：`src/modules/transit-cache/entities/transit-cache.entity.ts`
- TypeORM 迁移：`src/migrations/1741741000000-AddTransitCacheTable.ts`

## 8. 当前限制

- `transit_cache` 已拆分出 `walkingMinutes` 与 `transitProviderStatus`，`matrix-admin` 也已补充 fallback 与锚点诊断面板。
- 当前 backend 已把锚点、营业时间、预约、餐段、酒店入住退房和机场缓冲透传给 optimizer。
- optimizer 何时真正把这些字段纳入求解，取决于 optimizer 端后续 Phase 1+ 的实现进度。
